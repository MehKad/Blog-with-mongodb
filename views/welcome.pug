html
   head
      title Welcome
   body
   if username
      h1 Welcome, #{username}!
      a(href="/logout") Logout
      br
      a(href="/addArticle") Add new article
   else 
      h1 Welcome
      a(href="/login") Login
